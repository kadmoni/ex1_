#include "RLEList.h"
#include <stdlib.h>

#define NODE_INFO 3
#define EMPTY_POINTER -1

// כותבים כל מה שצריך בתוך התוכנית יש להם ifndef בפנים כנגיע ללינקר

RLEList asciiArtRead(FILE* in_stream)
{
    open??
    char* fgets(char* buffer, int size, FILE* stream)
}

RLEListResult asciiArtPrint(RLEList list, FILE *out_stream)
{
    int aAa = fputs(const char* str, FILE* stream);
    if (aAa =!)
        return fasfs;
}


RLEListResult asciiArtPrintEncoded(RLEList list, FILE *out_stream)
{
    int aAa = fputs(RLEListExportToString(RLEList list, RLEListResult* result), FILE* stream);

}


RLEListExportToString(RLEList list, RLEListResult* result)
